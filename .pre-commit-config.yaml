# This file is intentionally stubbed.
# Pre-commit behavior is defined as LLM instructions in system/modules/16-pre-commit-behavior.txt.
# If you need an actual hook config for this repo, generate one following the rules in that module.
